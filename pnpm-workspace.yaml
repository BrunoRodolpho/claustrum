packages:
  - "packages/*"
  - "examples/*"
  - "apps/*"
  # @adjudicate/* are consumed from the npm registry via each package's
  # ^1.1.0 ranges (resolves the published 1.3.x). The previous sibling
  # workspace link (../adjudicate/packages/*) was removed at the 1.3.0 cutover.
