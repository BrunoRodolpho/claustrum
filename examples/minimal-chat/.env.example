# minimal-chat — environment variables
#
# When set, src/index.ts wires a real AnthropicProvider against the Messages
# API. When unset, the example falls back to an in-memory model double so
# the build/test path stays hermetic.

ANTHROPIC_API_KEY=
