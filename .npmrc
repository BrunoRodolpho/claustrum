auto-install-peers=true
strict-peer-dependencies=false
# Link sibling @adjudicate/* (declared as semver ^1.1.0, not workspace:*) to the
# live ../adjudicate working tree so kernel fixes compose. See pnpm-workspace.yaml.
link-workspace-packages=true
prefer-workspace-packages=true
